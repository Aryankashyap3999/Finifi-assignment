import { ComingSoonPanel } from '@/components/layout/ComingSoonPanel';

export default function DeliveryTabPage() {
  return (
    <ComingSoonPanel
      title="Delivery (GRNs)"
      description="Per-GRN sub-tabs, form panel, file preview, and item grid land here next."
    />
  );
}
